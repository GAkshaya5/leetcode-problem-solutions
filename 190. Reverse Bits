class Solution:
    def reverseBits(self, n: int) -> int:
        res = 0
        for i in range(32):
            # Extract the last bit of n
            bit = (n >> i) & 1
            # Shift the bit to its new reversed position
            res = res | (bit << (31 - i))
        return res
